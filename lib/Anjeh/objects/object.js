class object
{
    vertices;
    indices;
    colors;

    constructor (_vertices, _indices, _colors)
    {
        this.vertices = _vertices;
        this._indices = _indices;
        this.colors = _colors;
    }
}